#include <iostream>
#include <vector>
#include <cstring>
#include <cmath>
#include <cstdio>
#include <algorithm>
#include <fstream>
#include <list>
#include <map>
#include <string>
#include <functional>
#include <stack>
using namespace std;
void mergeTwolists(vector<int>& list1, vector<int>& list2){
    // for (int i = 0; i < (sizeof(list1)/sizeof(list1[0])); i++)
    // {
    //     finallist[i] = list1[i];
    // }
    
}
int main() {
 vector<int> list1 = {1,2,4};
vector<int> list2 = {1,3,4};
mergeTwolists(list1,list2);
return 0;
}